# Copier ce fichier en k8s/secret.yaml et renseigner la vraie clé.
# secret.yaml est dans .gitignore — ne jamais commiter la vraie clé.
apiVersion: v1
kind: Secret
metadata:
  name: groq-secret
  namespace: ai-chatbot-web-service
type: Opaque
stringData:
  GROQ_API_KEY: "YOUR_GROQ_API_KEY_HERE"
